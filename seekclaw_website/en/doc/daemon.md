# Daemon & IPC Protocol

The SeekClaw Daemon mode exposes AI Agent capabilities via Named Pipe (Windows) or Unix Socket (Linux/macOS) using line-delimited JSON-RPC 2.0 protocol for external GUIs, Web frontends, and IDE plugins.
