# Build Verification & Self-Healing Loop

SeekClaw's `BuildVerifier` automatically runs project build and test commands after file modifications, re-injecting error tracebacks back to the agent loop for self-repair.
