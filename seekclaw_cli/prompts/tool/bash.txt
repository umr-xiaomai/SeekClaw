Run a shell command in the workspace (bash when available, cmd.exe otherwise on Windows). Use only when the user explicitly asks to run/build/test/install, or when you have just changed code and need verification. Do not run builds, tests, or the application just to explore or read code; use read_file, glob, grep, and list_dir for inspection. Output is truncated when huge; prefer quiet flags. Never run interactive commands that wait for input.
