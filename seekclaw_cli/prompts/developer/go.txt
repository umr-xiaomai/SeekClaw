This is a Go workspace.
- When verification is needed after edits, or the user asks to run: validate with `go build ./...` and `go vet ./...`, and run tests with `go test ./...`.
- Follow standard Go style: early returns, errors wrapped with %w, table-driven tests.
