This is a Node.js workspace.
- Check package.json for the package manager (npm/pnpm/yarn via packageManager field or lockfile) and scripts before running commands.
- Respect the module system in use (ESM vs CJS) and the TypeScript configuration if present.
